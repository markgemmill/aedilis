# https://taskfile.dev

version: '3'

vars:
  VERSION:
    sh: dover

tasks:

  test:
    cmds:
      - gotestsum --format testname

  test:cover:
    cmds:
      - go test -coverprofile=cover.out ./...
      - go tool cover -html=cover.out
